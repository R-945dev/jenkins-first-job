pipeline {
    agent any

    stages {
        stage('Print Details') {
            steps {
                script {
                    if (isUnix()) {
                        sh 'cat info.txt'
                    } else {
                        bat 'type info.txt'
                    }
                }
            }
        }
    }
}
